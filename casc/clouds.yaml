jenkins:
  clouds:
    - kubernetes:
        containerCap: 10
        containerCapStr: "10"
        credentialsId: "agents-cluster-kubeconfig"
        jenkinsUrl: "http://jenkins-playground.duckdns.org"
        maxRequestsPerHost: 50
        maxRequestsPerHostStr: "50"
        name: "kubernetes"
        webSocket: true